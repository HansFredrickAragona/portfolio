# Prompt 00 — Bootstrap Orchestrator

Execute only after explicit owner approval to run Prompt 00. Reviewing or editing this prompt is not execution authorization.

If the owner explicitly authorizes the prepared `portfolio-agent-system/optional/overnight-run.md` batch, that includes Prompt 00 and permits continuation to Prompt 01 after bootstrap checks instead of the final stop below. Record that exact batch scope in the generated contract/approval ledger. Subsequent Gates 1-7 become documented local-draft checkpoints under that contract; final owner review remains pending. Without batch activation, retain the normal stop behavior.

Act as lead orchestrator. Read `portfolio-agent-system/README.md`, all files under `portfolio-agent-system/rules/` and `portfolio-agent-system/specs/`, and existing root memory documents before acting. All generated paths below are relative to the portfolio project root. Inspect Git only if initialized; the owner initializes Git in this project. Never run `git init`. If Git is absent, record it and proceed only with approved documentation work after confirming state consistency.

Create root `AGENTS.md` that enforces the complete README startup order, simple MVC constraints, autonomy limits, specialist roles, approval gates, one-task-per-commit rule, privacy rules, and required checks. Create missing root `memory/` files using concise headings: `CURRENT_STATE.md`, `HANDOFF.md`, `TASKS.md`, `BLOCKERS.md`, `CONTENT_GAPS.md`, `APPROVALS.md`, `DECISIONS.md`, and `OPENWOLF.md`. Preserve existing records. Record actual owner approvals and the exact next authorized action; never manufacture historical approvals.

Verify or add root `.gitignore` exclusions for `project-input/`, secrets, and private/generated artifacts before any staging. Inspect staged paths and contents before committing. Do not copy private source contents into memory.

Include the agent role definitions, overnight policy, and three-day delivery target from `portfolio-agent-system/rules/AGENT-OPERATIONS.md` in the generated contract and task plan. Record the confirmed kickoff/deadline if supplied, otherwise mark exact dates pending. Do not infer authorization to cross gates, start scheduled automation, or deploy from the delivery target.

Define all six logical roles: Orchestrator, Design, Content, Programmer, QA, and Progress & Handoff. Preserve the single-writer memory ownership rules and the orchestrator's responsibility to review documentation, record owner approvals, and perform Git mutations. Rotate roles within the runtime's available concurrency rather than requiring six simultaneous agents.

Enforce a maximum delegation depth of one layer: only the root orchestrator creates subagents, and subagents never create further agents or sessions to delegate work. Subtasks needing division return to the root orchestrator. Cap concurrency at four agents total including the root, or the runtime limit if lower. Include these limits in root `AGENTS.md` and every subagent assignment; the orchestrator cannot waive them.

Include the low-usage OpenCode successor policy in the generated contract. During bootstrap, verify readiness according to `portfolio-agent-system/optional/opencode-handoff.md`, recording unresolved provider/model, capacity, permissions, or recovery checks. Preserve the 25% preparation and 15% transfer reserves and exclusive writer ownership. Do not confuse OpenWolf memory integration with OpenCode execution. A runtime switch preserves current scope and cannot authorize another phase.

Inspect the installed OpenWolf version and its local documentation. Follow the privacy and recovery acceptance criteria in `portfolio-agent-system/rules/AGENT-OPERATIONS.md`. Initialize only a documented, compatible integration; test recovery in a clean supported session and document exact results in `memory/OPENWOLF.md`. Never invent commands or claim an unperformed recovery test. If unavailable or unsafe to enable, document the Markdown/Git fallback and disable autonomous edits until state consistency is confirmed.

Do not scaffold the application. Check document consistency, referenced paths (distinguishing planned outputs), approval boundaries, privacy, and the complete diff. Record formatting/lint/TypeScript/tests/build/responsive/accessibility checks as not applicable where no relevant application exists, with reasons. Report files created, OpenWolf verification, reconstructed project state, blockers, and Prompt 01 as the next prompt requiring owner authorization. Commit only if Git exists and all applicable documentation and staged-privacy checks pass; otherwise report the uncommitted documentation. Do not push or open a PR if deployment-trigger safety is unverified. Stop without starting Prompt 01.
