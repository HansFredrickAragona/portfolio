# Optional — Session Recovery

Do not edit application code. Follow the complete startup order in `portfolio-agent-system/README.md`, including root approval/decision records, blockers, content gaps, and OpenWolf verification. Inspect Git status/history only if initialized; never initialize it. Reconcile records with actual files into a concise startup summary: current phase, approval scope and references, branch/HEAD or Git absence, uncommitted changes, completed work, active task, blockers, content gaps, latest checks, and exact next authorized action.

If OpenWolf is unavailable, use the Markdown/Git fallback and resume only after reconstructed state is internally consistent. If sources conflict, block affected work and report the discrepancy. Unavailability alone is not a permanent block once fallback consistency is verified. Never claim an unperformed clean-session recovery test.
