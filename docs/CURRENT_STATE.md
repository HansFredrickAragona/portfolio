# Current state (docs mirror)

See `memory/CURRENT_STATE.md` for full operational state.

Active branch: **feat/v2** — Figma Make **Next.js 15** app with local Codex refinements (app tip `8cebd47`). Typecheck + production build pass. Full prior app on `feat/v1`. See `memory/CURRENT_STATE.md` and `memory/OPENCODE_HANDOFF.md`.
