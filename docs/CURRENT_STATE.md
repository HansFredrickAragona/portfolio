# Current state (docs mirror)

See `memory/CURRENT_STATE.md` for full operational state.

Active branch: **feat/v2** (synced with origin) — Figma Make **Next.js 15** drop-in integrated with v1 skills/About/Hero/link ports (source `ff89b2a`). Typecheck + production build pass. Full prior app on `feat/v1`.
