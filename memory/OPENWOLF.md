# OpenWolf verification

Observed 2026-09-22: installed npm package and CLI version 1.0.4, at `C:/Users/Hans/AppData/Roaming/npm/node_modules/openwolf`. Absolute launcher `C:/Users/Hans/AppData/Roaming/npm/openwolf.cmd --version` returned 1.0.4. Package README read locally using elevated read access after sandbox denied access.

README documents `openwolf init`, status, scan, and Claude Code lifecycle hooks. Init registers hooks and creates `.wolf/` memory/index files. It states Claude Code CLI is required. No supported Codex/OpenCode hook integration or safe private-input indexing exclusion was established from that documentation. Therefore no init, daemon, remote integration, or speculative hook was executed.

Fallback: root AGENTS + memory Markdown + Git. State reconstructed from actual workspace: input files exist and are ignored, no app scaffold, initialized owner Git on feature branch, approved overnight batch, public contact and year milestones confirmed. These records are internally consistent and permit approved edits under the documented fallback rule.

Clean-session Markdown recovery checks attempted through both selected OpenCode models, but provider HTTP 403 prevented responses. No clean-session or OpenWolf recovery pass claimed. Current Codex reconstruction is internally consistent; successor must verify its own recovery when capacity/access is available. OpenWolf does not keep an agent running.
